const filhas = ['Ana', 'Maria'];
const filhos = ['Hagen', 'Siegrified'];
const todos = filhas.concat(filhos, 'Alberich'); //Concatenação de dois arrays e adição de mais um elemento no parâmetro;
console.log(todos);

console.log([].concat([1,2],[3,4], 5)) // Também é possível usa-lo atavés da notação ponto;
